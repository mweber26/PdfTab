<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
   <item android:id="@+id/menu_single_page"
      android:icon="@drawable/ic_menu_singlepage"
      android:title="Single page"
      android:showAsAction="ifRoom|withText" />

   <item android:id="@+id/menu_continuous"
      android:icon="@drawable/ic_menu_continuouspage"
      android:title="Continuous page"
      android:showAsAction="ifRoom|withText" />
</menu>
